inch = float(input("Enter length in inches (negative value to quit): "))

while inch >= 0:
    cm = inch * 2.54
    print(f"{inch} inches is {cm} centimeters")
    inch = float(input("Enter length in inches (negative value to quit): "))

print("Program ended.")
