import random

def roll_dice(dice_max):
    return random.randint(1,dice_max)

dice_max = int(input("the number of sides): "))

while True:
    dice = roll_dice(dice_max)
    print(dice)
    if dice == dice_max:
        break